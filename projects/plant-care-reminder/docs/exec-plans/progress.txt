### [Current Focus]
`CARE-005` 进行中：下一步开始实现 due inbox，把 enabled task 按 overdue、today、upcoming 三组汇总出来，并让 `/todo` 真正承接提醒队列，而不是只作为 completion 入口占位页。

### [Key Decisions]
- `CARE-004` 没有提前落地 completion mutation；plant detail 里的 `Complete` 入口先明确导向 `/todo`，把真正的完成动作留给 `CARE-006`。
- plant detail 的 task UI 已从页面内联实现收口为 `TaskSection` / `TaskListItem`，后续 inbox 和 completion flow 可以复用同一套任务展示语义。
- due 文案、task type label、cadence/history 信息继续走现有 shared formatter / task contract，避免 detail 和 inbox 产生两套表述。

### [Blockers & Solutions]
- AppRouter smoke 套件里同一测试二次 `renderWithProviders` 会让相同按钮名同时存在；这轮把 `Complete` 点击断言收窄到目标元素，避免误判。
- 浏览器级验证依旧受宿主 Playwright 限制影响，所以 `CARE-004` 继续以 typecheck + smoke tests 为主，不额外引入视觉验证链路。

### [Verification]
- `cd app && npm run typecheck`
- `cd app && npm run test`
- 本轮新增覆盖：
  - plant detail 会渲染 `Enabled routines`、`Add routine`、`Edit`、`Complete` 入口
  - 从 plant detail 点击 `Edit` 会进入 `/plants/:plantId/tasks/:taskId/edit`
  - 从 plant detail 点击 `Complete` 会进入 `/todo`
- 验证结果：
  - `tsc --noEmit` 通过
  - `vitest run` 通过，`4` 个测试文件、`40` 个测试全部通过

### [Dead Ends]
- 不要在 `CARE-004` 顺手实现 `/todo` 的真实分组查询；那是 `CARE-005` 的明确范围。
- 不要在 `CARE-004` 就补 completion log 写入；那是 `CARE-006` 的后端边界。

### [Next Steps]
1. 进入 `CARE-005`，实现 `/todo` 的 overdue / today / upcoming 分组查询和页面结构。
2. 让 plant detail 的 `Complete` 入口落到真实 inbox，而不是只到占位页。
