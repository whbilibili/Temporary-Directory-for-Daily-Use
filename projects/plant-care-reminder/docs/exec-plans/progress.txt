### [Current Focus]
`CARE-006` 进行中：下一步把 inbox 和 plant detail 里已经露出的 `Complete` affordance 接到真实 completion mutation，补 completion log 写入，并在完成后实时重算 `nextDueAt`。

### [Key Decisions]
- `CARE-005` 的后端查询用 `listDueTasks` 在 Convex 侧直接分好 `overdue` / `today` / `upcoming` 三组，前端不再自行判断 urgency。
- upcoming 窗口按 MVP 合同固定为未来 `3` 天，且只返回 enabled 且父植物未 archived 的任务。
- 本轮只落 inbox 读取面和 row-level `Complete` affordance；真正的完成写入、日志和 next-due 重算仍由 `CARE-006` 接手。

### [Blockers & Solutions]
- `CARE-005` 合同列出的文件不包含 `AppShell.tsx`，但 `/todo` 当前仍是 placeholder route；因此本轮额外修改路由装配文件，把真实 `TodoPage` 接到 app shell。
- smoke harness 的 `queryResult` 只能一次性注入单个 `useQuery` 返回值；由于 `AppShell` 在测试里直接接收 `routeContext` props，本轮可以单独给 `/todo` 注入 due inbox query 结果，不需要改测试基建。

### [Verification]
- `cd app && npm run typecheck`
- `cd app && npm run test`
- `cd app && npx convex dev --once --typecheck enable`
- 本轮新增覆盖：
  - `/todo` 会渲染 `Overdue`、`Due today`、`Upcoming` 三组
  - 每个 due row 都带可见的 `Complete` 按钮
  - 没有未来三天任务时会展示 inbox empty state
- 验证结果：
  - `tsc --noEmit` 通过
  - `vitest run` 通过，`4` 个测试文件、`41` 个测试全部通过
  - `Convex functions ready!`，新增 `listDueTasks` query 通过服务端 typecheck

### [Dead Ends]
- 不要在 `CARE-005` 就偷跑 completion mutation；否则会和 `CARE-006` 的 completion log / realtime recompute 合同重叠。
- 不要让 due inbox 返回 archived 植物的任务；归档后提醒面应以植物生命周期为准。

### [Next Steps]
1. 进入 `CARE-006`，新增真实 `completeTask` 写路径，原子写 completion log 并 patch `plantTasks`。
2. 把 inbox row 和 plant detail 的 `Complete` 按钮都接到同一个 completion mutation，并验证完成后队列实时刷新。
